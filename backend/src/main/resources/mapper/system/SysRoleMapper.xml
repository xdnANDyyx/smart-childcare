<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhyyt.nursery.module.system.mapper.SysRoleMapper">

    <select id="selectRolesByUserId" resultType="com.zhyyt.nursery.module.system.entity.SysRole">
        SELECT r.* FROM sys_role r
        INNER JOIN sys_user_role ur ON r.role_id = ur.role_id
        WHERE ur.user_id = #{userId} AND r.del_flag = '0'
        ORDER BY r.role_sort
    </select>

    <select id="selectAllRoles" resultType="com.zhyyt.nursery.module.system.entity.SysRole">
        SELECT * FROM sys_role WHERE del_flag = '0' ORDER BY role_sort
    </select>

</mapper>
